defaults:
  # Only one module for now
  module: "Narrative_Synopsis"

  # Default author tag embedded in metadata
  author: "Kevin"

  # Where the recommended output file will go
  output_path: "/Users/kevnthompson/Downloads"

  # Versioning – single source of truth
  prompt_version: "v1.9d"
  spec_version: "0.2.2"
  script_version: "0.2.2"

modules:
  Narrative_Synopsis:
    description: >
      Generate a narrative synopsis focusing on flow, tone, narrative arc, and
      the emotional progression of the material.

    # Placeholder – we will insert the real EVALUATE content later
    evaluate_body: |
      [EVALUATE_BODY_PLACEHOLDER]
